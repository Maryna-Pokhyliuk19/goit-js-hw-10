//нам нужно создать функцию которая будет рендерить страну, выводя
//1. флаг и название страны
//2.столицу
//3.население
//4.языки

export function renderCountryMarkup(countryies) {
    for (const country of countryies) {
        const { name: official, capital, population, flags: svg, languages } = country;
    
   
    }
}